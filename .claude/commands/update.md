Final check all the codes and make it more correct, consistent, and efficient.

Change the version of the plugin. If it is a small update, version + 0.0.1. If it is a big update, version + 0.1.0.

Only change version numbers in `manifest.json`. Do not change other properties.

Update the maintenance guide and README.md. They are in the root of the repository.

At last, copy `main.js`, `manifest.json`, `styles.css` to the root of the project.