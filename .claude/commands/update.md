Final check all the codes.

Change the version of the plugin. If it is a small update (fixing small bugs), version + 0.0.1. If it is a big update (add some new feature), version + 0.1.0.

Only change version numbers in `manifest.json`. Do not change other properties in this file.

Update the maintenance guide and README.md. They are in the root of the repository.

At last, copy `main.js`, `manifest.json`, `styles.css` to the root of the project.